import React from 'react';
import {View, Text} from 'react-native';

const Login = props => {
  return(
    <View style={{flex: 1}}>
      <Text>Login</Text>
    </View>
  );
}

export default Login;
